export interface MonthlyRevenue {
  year: number | string;
  month: number | string;
  monthly_total: number | string;
}
export interface DailyRevenue {
  date: string | number;
  daily_total: string | number;
}
